sudo: required 
services: 
  - docker

before_install: 
  - docker build -t zencid/docker-react -f Dockerfile.dev .

script:
  #- docker run -e CI=true zencid/docker-react npm run test
  - docker run zencid/docker-react npm run test -- --coverage